<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <!-- Guard: fail the run (MTP exit code 9) if this project discovers 0 tests,
         so a broken test setup cannot silently green a CI run. -->
    <TestingPlatformCommandLineArguments>--minimum-expected-tests 1</TestingPlatformCommandLineArguments>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="TngTech.ArchUnitNET.xUnitV3" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <!-- Referenced only so their assemblies land in this project's output directory
       for reflection (a test seam, P-6). The dependency-direction rules are
       asserted over the compiled FcaBedrock.*.dll metadata. -->
  <ItemGroup>
    <ProjectReference Include="..\..\src\FcaBedrock.Diagnostics\FcaBedrock.Diagnostics.csproj" />
    <ProjectReference Include="..\..\src\FcaBedrock.Core\FcaBedrock.Core.csproj" />
  </ItemGroup>

</Project>
